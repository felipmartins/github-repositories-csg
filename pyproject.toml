[project]
name = "CSG International - Github Application"
version = "0.1.0"
description = "Github Integration for CSG International"
readme = "README.md"
requires-python = ">=3.10"

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-s -vv"
testpaths = [
    "tests",
]